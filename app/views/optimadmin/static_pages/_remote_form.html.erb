<% flash.each do |name, msg| %>
  <%= content_tag :div, msg, class: "flash-message #{name}" %>
<% end %>

<%= form_for @static_page, remote: true do |f| %>
  <% render 'optimadmin/static_pages/form_fields', f: f %>
<% end %>
